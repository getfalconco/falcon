/** Bump when extraction prompt or logic changes — invalidates candidates cache. */
export const EXTRACTION_VERSION = 3;

/** Bump on any prompt/logic change — invalidates full result cache. */
export const PIPELINE_VERSION = 5;
