#!/bin/sh
# Push öncesi Discord: "<sha> · <kişi> push etti (<branch>) · <özet>"
ROOT=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
cd "$ROOT" || exit 0
node scripts/discord-git-notify.mjs --action push >/dev/null 2>&1 || true
exit 0
