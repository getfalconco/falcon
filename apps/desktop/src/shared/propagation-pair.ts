/** The pair record — see `propagation-progress.ts` on why this is a re-export. */

export {
  OPEN_BELOW,
  PRICED_AT_OR_ABOVE,
  isResolved,
  pairOutcome,
  type PairHistoryEvent,
  type PairOutcome,
  type PropagationPairHistory,
  type PropagationPairHistoryResult,
} from "@meridian/research/propagation/contracts";
