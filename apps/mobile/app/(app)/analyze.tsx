import { View } from "react-native";
import { Screen } from "@/components/product-ui";

/**
 * Analysis — deliberately empty. The tab stays in the bar; what goes on this
 * page has not been decided yet, so it holds the slot rather than shipping a
 * screen nobody asked for.
 */
export default function AnalyzeScreen() {
  return (
    <Screen>
      <View />
    </Screen>
  );
}
